# ADR-0001：服务端 RecipeManager 是配方真源

- 状态：已接受
- 日期：2026-08-25

## 决策

核心从服务端 `RecipeManager` 与机器公开 API 建配方索引。JEI 只用于客户端展示、对比和交互入口。

## 原因

专用服务器通常不安装 JEI；JEI 分类也不包含完整的机器自动化语义。让核心依赖 JEI 会产生客户端可见、服务端不可执行的“幽灵样板”，并引入类加载崩溃风险。

## 后果

每种复杂机器需要显式适配器。开发成本更高，但行为可测试、可在专服运行、能正确处理输入事务与重载。
