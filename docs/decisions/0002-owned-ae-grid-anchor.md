# ADR-0002：使用自有 AE 网络锚点

- 状态：已接受
- 日期：2026-08-25

## 决策

第一版提供“全样板链接器”方块，由它拥有 `IManagedGridNode` 并注册 `ICraftingProvider`。绑定器选择该锚点，而不是把服务注入任意 AE 方块。

## 原因

自有锚点让频道、能量、安全、存档、拆除和迁移都落在本模组可控生命周期内，也避免 mixin/内部 API。

## 后果

玩家需要多放一个方块。以后可增加 AE 线缆部件变体，但不会改变绑定记录和 provider 核心模型。
