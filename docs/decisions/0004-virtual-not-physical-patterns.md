# ADR-0004：发布虚拟样板而非生成实体样板

- 状态：已接受
- 日期：2026-08-25

## 决策

链接器通过 AE2 `ICraftingProvider` 返回内存中的 `IPatternDetails`。不把自动发现的配方编码为真实样板物品并塞入隐藏库存。

## 原因

实体样板会增加存档、同步、去重和重载迁移成本。官方 crafting provider API 已经直接表达“这个节点能提供哪些样板”。

## 后果

虚拟样板只在链接器在线时存在，不能被玩家取出；UI 需要明确标识来源与离线状态。
