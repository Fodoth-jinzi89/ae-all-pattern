# AE All Pattern 更新记录（0.1.8 → 0.1.19）

> 基线：上游 `LangQi99/ae-all-pattern` v0.1.7（MIT）。
> 本文件记录相对该基线的全部改动，按版本倒序（最新在前）。
> 目标：Minecraft 1.21.1 / NeoForge 21.1.219 / AE2 19.2.17 / Java 21。

---

## 0.1.19 — Advanced AE 高级样板编码器可编辑灌注聚合样板

**需求**：Advanced AE 的高级样板编码器能打开并编辑灌注工厂生成的聚合样板；且**只有灌注类**可编辑，**未选中的子样板不得暴露给编码器**。

**实现**
- 新增 `aggregate/AggregatePatternEditPolicy`（策略类，刻意不放 mixin 包）：
  - `decodeForEditor(stack, level)`：解码后若是 marker 且非占位、且 catalyst 路径含 `infusing` → 逐层解包到首个**选中**子样板的 AE2 处理样板；否则原样返回 marker（编辑器认不出 → 不可编辑）
  - 灌注判定 = 库条目 `catalystId.getPath().contains("infusing")`，覆盖 mekanism / mekmm 各等级 `*_infusing_factory`
  - `unwrapForEditor` 最多解包 4 层（marker → AggregatePatternDetails → AE2 原生样板）
- `AdvPatternEncoderMenuMixin` 只做转发：Redirect `decodeInputPattern` / `copyItemToOutputSlot` 两处的 `PatternDetailsHelper.decodePattern`
- mixin 插件给 `AdvPatternEncoderMenuMixin` 加 `loaded("advanced_ae")` 门控
- "未选中不可读"天然满足：decode 走 `expandFirst`（已按 selection 过滤），解包链只含首个选中子样板

**验证**：新增 3 个 GameTest（infusingAggregateIsEditableByPatternEditor / nonInfusingAggregateStaysEditorLocked / editorNeverReadsDeselectedChildren），GameTest **34/34**；runClient 带 AdvancedAE-1.6.12 验证 0 mixin 失败。

---

## 0.1.18 — 机器→配方类别选错（"配方全不对 + 读不到"的真因）

**现象**：用户反馈灌注工厂生成的聚合样板是"输入 1 红石粉 → 输出 10 红石"、"1 富集钻石 → 钻石×80"；JEI 上 46 条实际到手 34 条，配方没一个对的，放进供应器完全读不到。

**根因**：`ClientJeiAggregateScanner.findNativeCategory` 只按 namespace 过滤后 `findFirst`。而 JEI 里一台机器常是**多个类别的 catalyst**——冶金灌注机同时是 `metallurgic_infusing`（产出配方）和 `infusion_conversion`（物品→灌注类型，给机器补灌注）的 catalyst，两者都是它的合法操作。`findFirst` 取到哪个全看 JEI 返回顺序，这次取到了转换类。

**修法**
1. `machineKeyword(path)`：剥 tier 前缀（basic_/advanced_/elite_/ultimate_/absolute_/supreme_/cosmic_/infinite_）+ 剥外壳后缀（_factory/_machine/_chamber），得到 `infusing`
2. `pickCategoryId`：优先 `类别路径.contains(keyword)`（`metallurgic_infusing` 含 `infusing` ✓，`infusion_conversion` 不含 ✗）
3. 兜底**最长公共前缀**（≥4 字符）：`metallurgic_infuser` ↔ `metallurgic_infusing`（公共前缀 15）、`enrichment_chamber` → `enriching`（6）
4. 最后才退回原 findFirst，保证未知机器仍可扫描

**验证**：新增 `JeiCategorySelectionTest`（6 用例，含"infusing_factory 必须选 metallurgic_infusing"回归断言；注意被测方法是包私有，测试类必须放 `client` 包）。单测 192 全过、GameTest 31/31。

**遗留**：含化学品（appmek `appmek:chemical`）输入的配方能否展开成功尚未实证——化学品转换依赖 `ae2jeiintegration` 的 `IngredientConverters`，该模组缺失时含化学品的配方会被静默跳过。

---

## 0.1.17 — 修 0.1.16 崩溃（mixin 包硬规则）

**现象**：0.1.16 装进整合包后进不去世界，报 `IllegalClassLoadError: ...mixin.Reflect is in a defined mixin package ... and cannot be referenced directly`。

**根因（必须记住的规则）**：`mixins.json` 里 `package` 声明的包被该配置"占有"，**包下所有类都不允许被已转换的目标类在运行时引用**。我把反射工具类 `Reflect` 放进了 `...mixin` 包（图省事与 mixin 类同包），三个新 mixin 转成目标类后执行注入代码 → 引用 `Reflect` → 抛 Error（**是 Error 不是 Exception，业务 try/catch 拦不住**）→ 崩溃。

**修法**：移到 `io.github.langqi99.aeallpattern.util.Reflect`。mixin 包下只允许放 `@Mixin` 类和 mixin plugin。
**自查命令**：`grep -rn "io\.github\.langqi99\.aeallpattern\.mixin\.[A-Z]" src/main/java` 应无输出。

**验证方法的重要教训**：runClient **新建世界**里根本没有猪咪供应器方块，`updatePatterns` 那条路径压根没执行——**mixin 应用成功 ≠ 注入代码被执行**。真正能验证的做法：把 ae2lt + thunderbolt 复制到 `C:\aap\run-gametest\mods`，写 GameTest 放置该方块并反射调用 `updatePatterns`，并在注入点加 `LOGGER.info` 探针**确认探针真的输出**再断言（否则无法区分"通过了"和"没跑到"）。新增的 `CoreGameTests.addonProviderMixinsStayLoadable` 就是这个模式，未装 ae2lt 时自动 succeed。

**结果**：版本升到 0.1.17（坏的 0.1.16 作废），GameTest **31/31**，探针确认猪咪供应器注入点执行 2 个且 `IllegalClassLoadError` 0 次。

---

## 0.1.16 — 附属供应器只读到聚合样板的第一个子配方（本版已作废，见 0.1.17）

**现象**：猪咪样板供应器、高级样板供应器、高级扩展样板供应器读聚合样板时只认第一个子配方。

**定位方法（可复用）**
1. 用 Python 遍历整合包每个 jar 的 `assets/*/lang/zh_cn.json`，按中文名搜关键词定位 modId 与方块 key：
   - 猪咪样板供应器 → `ae2lt` 的 `block.ae2lt.pigmee_pattern_provider`
   - 高级样板供应器 → `advanced_ae` 的 `block.advanced_ae.small_adv_pattern_provider`
   - 高级扩展样板供应器 → `advanced_ae` 的 `block.advanced_ae.adv_pattern_provider`
2. 再扫所有 class 的字节，找同时含 `PatternDetailsHelper` + `decodePattern` 的类 → 列出"自己抄了一份样板列表逻辑"的宿主（AE2 本体除外）。这是发现同类问题最快的方式。

**根因（这一类问题的通病）**：`AggregatePatternDecoder.decodePattern` 只返回 `expandFirst()`，因为槽位校验付不起全量展开的代价。凡是**自己遍历 inventory → `PatternDetailsHelper.decodePattern` → add 一个**的宿主，都只看到第一个子配方。AE2 原版 `PatternProviderLogic` 有 mixin 覆盖，但这些是各自抄了一份：

| 宿主类 | 模组 | 说明 |
|---|---|---|
| `com.moakiee.ae2lt.blockentity.PigmeePatternProviderBlockEntity` | ae2lt | 猪咪样板供应器 |
| `net.pedroksl.advanced_ae.common.logic.AdvPatternProviderLogic` | advanced_ae | 高级 + 高级扩展共用（Small 继承自大的） |
| `com.moakiee.ae2lt.packaged.patternprovider.StablePatternProviderLogic` | ae2ltpp | 封包/无线封包供应器；**继承 AE2 原版 logic 但覆写 updatePatterns 且不调 super**，原版 mixin 对其无效 |

ExtendedAE 的 `TileExPatternProvider` 不在此列（用 AE2 原版 logic，已被覆盖，所以用户没报它）。

**修法**
- 三个 mixin 均在宿主 `requestUpdate` **之前**注入（保证完整列表先就位再通知网络）
- `AdvPatternProviderLogicMixin` 顺带补 `craftingWatcher`/`outputCache`/`patternInputs`，保住锁定合成功能
- 共用基础设施：`AggregateProviderExpansion.expandSlots(inventory, level, onCompletion, sink)` + `util/Reflect`
- **防无限循环**：mixin 加 `@Unique boolean aeallpattern$rerunning`，冷启动展开完成后 rerun 宿主 `updatePatterns` 时把 `onCompletion` 传 **null**（避免"展开结果为空"即全部取消选择时无限自我调度）
- TAIL 注入时先 `patterns.removeIf(AggregatePatternMarkerDetails.class::isInstance)` 删掉宿主刚解码的 marker，再用完整展开列表替换，避免第一个子配方重复

---

## 0.1.15 — 打开 AE 终端卡顿

**现象**：聚合样板展开成 18509 条后，打开 AE 终端卡顿。

**三个根因（查 AE2 源码确认）**
1. **展开缓存太小导致反复全量重算**：`RECIPE_EXPANSION_CACHE` 上限 4096 < 18509 配方，展开后大量条目被驱逐，每次打开终端（CraftingService 枚举网络样板）又要重新 encode+decode 全量展开
2. **供应器每次刷新都触发网络重建**：`PatternProviderLogicMixin` 每次 `updatePatterns` 无条件 `requestUpdate` → AE2 每次都要重建 18509 条索引
3. **附属模组同步全量展开**：矩阵端口 / ECO 总线 / 超载供应器的 mixin 在打开终端瞬间同步展开全部 18509 条，直接卡住服务器线程

**修复**
1. `RECIPE_EXPANSION_CACHE` 4096 → **200_000**；`EXPANSION_CACHE` 32 → **256**（远超任何聚合配方数，整段展开结果全量驻留缓存不驱逐）
2. `PatternProviderLogicMixin`：记录上次发布结果 `aeallpattern$lastPublished`，展开结果**未变化时不 requestUpdate**
3. 三个附属 mixin 同步 `expand` → `expandScheduled`（分 tick 渐进展开，每 tick 2ms 预算），完成后重跑 rebuild 发布完整结果（Matrix/ECO 用反射 rerun，Overloaded 用 `@Invoker("rebuild")` + 保存最近参数）

---

## 0.1.14 — 修 0.1.13 启动崩溃（@Shadow 类型不匹配）

**现象**：0.1.13 装进整合包进不去游戏，日志深处：
```
Mixin apply for mod aeallpattern failed ... MatrixPatternStorageBlockEntityMixin
→ @Shadow field inventory was not located in the target class ...
```

**根因**：我反编译的是桌面汉化包目录的 ae2lt jar，与整合包实际使用的 jar 字段类型不一致——整合包里 `inventory` 字段类型是内部类 `PatternInventory`，不是 `AppEngInternalInventory`，`@Shadow` **类型必须完全匹配**，不匹配即报 "not located" → mixin 应用失败 → 启动崩溃。本地 runClient 没装 ae2lt，mixin 被 plugin 条件跳过，所以没暴露。

**修法**：改为纯反射（`getDeclaredField` / `getInventory()` / `getSlots()` / `getStackInSlot(int)`）。注意：`getMethod` 传原始类型参数要用 `int.class` 而非 `Integer.class`。

---

## 0.1.13 — 天枢物质扭曲矩阵 + 附属模组样板兼容

**问题1（矩阵端口放不进）**：AE2 闪电科技（ae2lt 2.0.9）的"天枢物质扭曲矩阵"矩阵端口完全放不进聚合样板，会被弹出来，不论选择不选择。

根因：`MatrixPatternStorageBlockEntity.isValidPatternStack`：
```java
if (stack.isEmpty()) return true;
if (level == null) return PatternDetailsHelper.isEncodedPattern(stack);
IPatternDetails d = PatternDetailsHelper.decodePattern(stack, level);
return d instanceof IMolecularAssemblerSupportedPattern;   // ← 聚合样板解码出的 marker 没实现该接口
```
`rebuildPatternCache` 也只收集 `instanceof IMolecularAssemblerSupportedPattern` 的样板，所以即使放进去也不会被识别。

**修复**
- `AggregatePatternMarkerDetails` **实现 `IMolecularAssemblerSupportedPattern`**：`assemble`/`getRemainingItems`/`isItemValid`/`isSlotEnabled`/`fillCraftingGrid` 转发给内部子样板；子样板不是合成类时安全兜底（EMPTY / false / 空操作）
- 新增 `MatrixPatternStorageBlockEntityMixin`：`rebuildPatternCache` TAIL 时展开聚合样板，把每个 `IMolecularAssemblerSupportedPattern` 子配方加入 `cachedPatterns`，并移除 marker

**问题2（只识别第一个 / 全选放不进单选能放）**
- "只识别第一个"根因：`AggregatePatternDecoder.decodePattern` 返回 `expanded.getFirst()`，只代表第一个子配方
- "全选放不进、单选能放"根因：`decodePattern` 在插入校验瞬间**同步全量展开 18509 条**（全选时 selection 为 null 展开全部，卡顿超时被拒；单选只展开几条，快）

**修复**：新增 `AggregatePatternExpander.expandFirst(stack, level)`（只展开第一个可发布子配方，优先走缓存），`AggregatePatternDecoder.decodePattern` 改用它。

---

## 0.1.12 — 全量搜索（服务端过滤 + 分页回传）

**问题**：搜索只在服务端同步的前 1024 条配方（`MAX_SYNCED_ENTRIES`）里生效，18509 条配方的聚合样板搜不全。

**方案**：搜索从客户端本地过滤改为**服务端全量过滤 + 分页回传**
- 新增 `AggregatePatternSearch`（公共搜索类，服务端用，从 Screen 提取的 AE2 语法逻辑）
- 新增 `AggregateSearchPayload`（客户端→服务端：requestId + 搜索词 + 输入/输出模式）
- 新增 `AggregateSearchResultPayload`（服务端→客户端：requestId/pageIndex/pageCount/每页 ≤64 条，避免超协议帧上限）
- `Menu.applySearch(ServerPlayer, text, outputs, requestId)`：从 `AggregatePatternLibrary` 取完整配方列表 → `AggregatePatternSearch.filter`（limit=`Integer.MAX_VALUE`，**不再截断**）→ 分页 `sendToPlayer`
- `Screen`：防抖 250ms 后 sendSearch，requestId 判过期，收齐所有页后 `menu.updateEntries(flat)` 替换显示

**两个编译坑**
1. `AggregatePatternSelectionScreen.receiveSearchResult` 是包私有，network 包调用报错 → 改 `public`
2. **MC 1.21.1 的 `AbstractContainerScreen.tick()` 是 `final`**，不能 `@Override` → 改用 `containerTick()`（protected，父类 final tick 每 tick 调用它）

---

## 0.1.11 — 选择界面搜索 + 缩小全选按钮

- **搜索栏**（顶部新增一行，GRID_TOP 26→50、imageHeight +24）：右上角 [输入][输出] 两个模式 Tab（自绘，当前模式深紫高亮）+ EditBox（100×16）
- **搜索语法与 AE2 终端一致**：空格分词 AND、`!` 取反、`@`模组 id/名、`$`物品/流体 tag、`=`注册表 id、`#`同 id、裸词匹配显示名（大小写不敏感）
- 输入模式匹配 entry 的 inputs、输出模式匹配 outputs（任一物品命中即显示）
- ESC 在搜索框聚焦时先取消焦点不关界面；过滤后滚动条/计数/空结果提示都基于过滤集
- 全选按钮 88×20 → **66×18**
- 新增 lang 键：search_by_input / search_by_output / search_no_results / search_hint（中英）

**说明**：AE2 的 `RepoSearch` 谓词类绑定 `GridInventoryEntry`（终端条目），无法直接复用，故照抄其语义自行实现。

---

## 0.1.10 — 大配方库扫描提速 + 生成失败修复

**现象**：整合包工作台 18509 条配方，扫描 10 分钟，到 16000 左右停住不提示也不生成。

**根因1（生成失败）**：服务端距离校验 `AggregateGenerationService.validTarget` 要求 `player.distanceToSqr(machinePos) <= 64`。跨 tick 扫描 10 分钟玩家必然走开 → 所有上传页被拒 → 永不生成。
**修复**：去掉距离限制（保留机器方块 / catalystId / machineKey 校验 + 手持生成器校验，安全性足够）。

**根因2（扫描慢）**：crafting 走了最贵的 JEI 布局路径（18509 条 × `createRecipeLayoutDrawable` + 每条 tooltip 解析 + 全量扫 tag）。
**修复**：
- **vanilla 快速路径**：CRAFTING / STONECUTTING 类别改用 `RecipeManager` 直接解析（`Ingredient`→`AggregateInputSlot`、`getResultItem`→输出），在**后台线程**跑（纯数据只读，不碰渲染），完成回主线程 `execute()` 上传。18509 条 **<2 秒**，零卡顿
- JEI 路径通用优化：`exactItemTag` 全量扫 tag 加 LRU 缓存（256 条）；`isProbabilistic` 的 tooltip 解析仅 PROCESSING 类别做（crafting 无概率）
- 截断提示：配方数超 `MAX_RECIPES`(16384) 时提示"已生成前 N 条"
- 服务端上传超时 30s → 60s 保险

**API 注意**：1.21.1 `RecipeManager.byType` 泛型不匹配编译不过，改用 `getAllRecipesFor`；`StonecutterRecipe` 是 `SingleItemRecipe`（无 `getIngredient()`），用 `getIngredients().getFirst()`。

---

## 0.1.9 — 大配方库扫描不出聚合样板

**现象**：几千/几万条配方的机器右击生成，只显示"后台扫描生成"文字，实际不生成。

**根因1**：`scanOne` 无异常保护。分帧扫描逐条构建 JEI 布局，几万条中任一条异常会直接抛穿 `step()`，`activeJob` 悬挂（永不 finish、永不置空），反复右击都一样。
**修复**：`step()` 内 try-catch 单条跳过 + `LOGGER.debug`。

**根因2**：固定 128 条/页超协议 32KB 帧上限。复杂配方（多输入/流体/概率输出/alternatives）单条约 0.5~2KB，128 条 = 几十~百 KB，超 Minecraft/NeoForge 帧上限（32767 字节），发送端超限服务端收不到完整分页 → 30 秒超时丢弃 → 不生成。
**修复**：`AggregateRecipe` 新增 `encodedSizeEstimate()`（保守上界，varint/utf/RL/components 192 字节垫），客户端 `finish()` 按 **20KB 字节预算**切页（页数不再固定），`GenerateAggregatePayload` 的 `pageCount` 上限从 `ceil(16384/128)=128` 放宽到 `MAX_RECIPES`。

**附带体验**：新增 `scan_progress` 语言键（"正在扫描配方：x / y"），每 400 条 actionbar 提示一次进度。

---

## 0.1.8 — 聚合样板选择窗口 + 三轮性能工作

### 聚合样板选择窗口（蹲下右键打开）
- 新增 `aggregate/AggregatePatternSelection`：data component record，`inverted + ids` 双模式存储（全选=删组件、全不选=inverted+空表，两端都极小）
- 新增 `AggregatePatternSelectionMenu`：服务端权威 + 客户端乐观更新；`clickMenuButton` 负数 id 为批量操作（-1 全选 / -2 取消全选），≥0 为按下标切换
- 新增 `client/AggregatePatternSelectionScreen`：滚动列表 + 滚轮 + **滚动条拖拽**、**斜切对分图标**（输入左上 8×8、输出右下，逐像素画对角线分隔）、已选排前（粉 `0xFFF7CBE0` / 灰 `0xFFCFCFD8`）、底部动态全选按钮；后续迭代把 SLOT_SIZE 18→26、ICON_SCALE 0.5→0.75（图标 8px→12px）
- 改 `AggregatePatternItem.use()`：shift 分流到选择菜单；tooltip 加已选计数
- 改 `AggregatePatternExpander.expand()`：按 selection 过滤 + 选择哈希混入虚拟样板 ID（触发供应器刷新）

### Bug：占位样板泄漏
全取消后放入供应器，终端出现"圆石→石头"的假样板。根因：`AggregatePatternDecoder.decodePattern` 在展开结果为空时返回包着占位样板（COBBLESTONE→STONE）的 marker，本意是让 AE2 继续接受该物品放进槽位；但 `PatternProviderLogicMixin` 的 fallback `decodePattern` 把它直接 add 进发布列表。
**修复**：marker 加 `placeholder` 标志 + `isPlaceholder()`；Decoder 空展开时返回 `new AggregatePatternMarkerDetails(key, standIn, true)`；mixin fallback 遇到 `isPlaceholder()` 直接 continue（槽位有效性保留、但不发布）。

### 性能三轮（这条主线最终在 0.1.15 收敛）
1. **展开缓存**：新增 `EXPANSION_CACHE`（WeakHashMap<server> + LRU 32），键 = libraryId + 配方代数 + contentHash + recipeCount + options.flags + selectionHash
2. **分帧化**：新增 `expandScheduled(stack, level, onCompletion)` + `tickServer(server)`（每 tick 2ms / 256 步）；客户端 JEI 扫描重构为 `ScanJob`（每 tick 2.5ms），完成后分页上传
3. **linker 路径**：新增 `RECIPE_EXPANSION_CACHE`（LRU 4096）；`refresh` 改为 `RefreshJob` 跨 tick 构建（每 tick 2ms / 128 条），期间继续发布旧 routes，完成时原子替换 + requestUpdate；`IncomingBuffer.hasWork/canAccept` 改 O(1) workCount 计数，`drainBoundMachineOutput` 改用 `BindingSavedData.byAnchor`

**NeoForge 21.1 tick 事件类**：`net.neoforged.neoforge.event.tick.ServerTickEvent.Post` / `net.neoforged.neoforge.client.event.ClientTickEvent.Post`。

---

## 附：提交 PR 前已处理 / 需注意的事项

1. ✅ **`gradle.properties` 的本机 JDK 路径已注释**（`org.gradle.java.home`）——保留注释供本机开发时取消，上游保持默认行为。
2. ✅ **`gradle/wrapper/gradle-wrapper.properties` 已还原官方 distributionUrl**——镜像地址以注释形式保留，本机开发时可自行切回。
3. 版本号同步：`build.gradle` 的 `verifyReleaseMetadata` 任务要求 `README.md` 与 `docs/product/support-matrix.md` 同时更新版本号（本包内已同步为 0.1.19），否则 build 失败。
4. 中文路径下 Gradle 的 `test` 任务必失败（classpath 传参被破坏），CI/评审者需在纯 ASCII 路径下跑测试（如 `C:\aap`）。
5. `run/mods` 与 `run-gametest/mods` 中的附属模组（ae2lt、thunderbolt、AdvancedAE、ae2ltpp、PackagedAuto 等）为本地验证环境，**不在本包内**；未装这些模组时相关 mixin 会被 plugin 条件跳过，`addonProviderMixinsStayLoadable` 等 GameTest 自动 succeed。

---

## 附：PR 描述草稿（可直接粘贴）

**标题**：Aggregate pattern fixes & add-on compatibility: selection UI, search, matrix port, provider expansion, terminal performance (0.1.8 → 0.1.19)

**正文**：

基于 v0.1.7 的增量改进，涵盖四大主题：

### 1. 聚合样板选择窗口（0.1.8 / 0.1.11 / 0.1.12）
- 潜行右击聚合样板打开选择窗口：逐个启用/禁用子配方、已选排前、滚动条拖拽、输入/输出斜切对分图标、全选/取消全选
- 搜索栏与 AE2 终端语法一致（`@模组` `$标签` `=id` `!排除` 空格 AND），输入/输出两种模式
- 搜索改为服务端全量过滤 + 分页回传，覆盖全部存储配方而非同步的前 1024 条

### 2. 大配方库扫描（0.1.9 / 0.1.10 / 0.1.18）
- 修复 18509 条配方的机器扫描失败：单条异常保护 + 分页改按 20KB 字节预算切页（原固定 128 条超协议帧上限）
- vanilla 合成/切石配方走 RecipeManager 快速路径（后台线程，18509 条 < 2 秒）；去掉跨 tick 扫描期间的 8 格距离校验
- 修复 JEI 类别选择：机器常是多个类别的 catalyst（冶金灌注机同时是 `metallurgic_infusing` 与 `infusion_conversion`），改为按机器 id 关键词匹配 + 最长公共前缀兜底

### 3. 附属模组兼容（0.1.13 / 0.1.14 / 0.1.16 / 0.1.17 / 0.1.19）
- 解码 marker 实现 `IMolecularAssemblerSupportedPattern`，聚合样板可放入 AE2 闪电科技"天枢物质扭曲矩阵"矩阵端口；新增矩阵样板仓展开 mixin
- 修复 Pigmee（ae2lt）、高级/高级扩展供应器（Advanced AE）、封包供应器（ae2ltpp）只读到第一个子配方的问题——这些宿主各自抄了一份样板列表逻辑，统一在其 `requestUpdate` 之前注入完整展开列表
- Advanced AE 高级样板编码器可编辑灌注工厂聚合样板（仅灌注类可编辑，未选中子样板不暴露）
- 对附属模组的 mixin 一律使用反射访问宿主字段/方法，避免跨版本 `@Shadow` 类型不匹配导致启动崩溃；工具类严格放在 `util` 包（mixin 配置声明的包不允许被目标类运行时引用）

### 4. 性能（0.1.8 / 0.1.15）
- 服务端展开缓存扩容至 200_000 条（单配方）+ 256 条（整库），覆盖全量多千配方聚合整段会话不驱逐
- 供应器展开结果未变化时不触发网络更新；附属宿主同步全量展开改为分 tick 渐进展开（每 tick 2ms 预算）

### 验证
- 单元测试全过；GameTest **34/34**（含针对附属宿主的可加载性与展开路径测试）
- 客户端在包含 ae2lt / ae2ltpp / AdvancedAE / PackagedAuto 的环境下启动验证，`Mixin apply failed` 计数为 0

CHANGELOG 已按上游格式追加 0.1.8–0.1.19 条目；详细技术细节与踩坑记录见本仓库 `更新记录-0.1.8-0.1.19.md`。
